export type Contact = {
  name: string;
  phone: string;
  email: string;
};

export type TaskNotificationPayload = {
  title: string;
  description: string;
};

export type AssetNotificationPayload = {
  url: string;
  username: string;
};
